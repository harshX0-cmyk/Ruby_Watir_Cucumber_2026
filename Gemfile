# frozen_string_literal: true # rubocop:disable Layout/EndOfLine

source 'https://rubygems.org'

# Core automation gems
gem 'cucumber'
gem 'selenium-webdriver'
gem 'watir'

# Debugging / testing
gem 'pry'
gem 'rspec'

# Development tools and linters - These gems are ONLY for development (not needed for running tests in production/CI unless explicitly included) # rubocop:disable Layout/LineLength
group :development do
  gem 'rubocop', require: true
  gem 'ruby-lsp', require: true
end
